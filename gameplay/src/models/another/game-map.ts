export default class Map {
    name: number
    des: string
    hash: string
    src: string
    
    constructor(name: number, des: string, hash: string, src: string) {
        this.name = name
        this.des = des
        this.hash = hash
    }
}
